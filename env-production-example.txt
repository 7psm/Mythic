# Variables d'environnement pour la PRODUCTION
# Ce fichier doit être configuré sur Render.com ou votre hébergeur

# Configuration Email Gmail
GMAIL_USER=votre-email@gmail.com
GMAIL_APP_PASSWORD=votre-mot-de-passe-app-gmail

# Configuration Serveur
PORT=10000
NODE_ENV=production
HOST=0.0.0.0
SERVER_TIMEOUT=30000

# Configuration CORS (URL de votre frontend Netlify)
CORS_ORIGIN=https://mythicmarket.netlify.app

# Configuration Logging
LOG_LEVEL=warn
EMAIL_LOGS=true
LOG_TO_FILE=false

# Configuration Email Templates
EMAIL_TEMPLATE_CACHE=true
EMAIL_CACHE_SIZE=100
DEFAULT_LANGUAGE=fr

# Configuration Email Avancée
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_POOL=true
EMAIL_MAX_CONNECTIONS=5
EMAIL_MAX_MESSAGES=100
EMAIL_RATE_LIMIT=14
EMAIL_RATE_DELTA=1000

# Configuration TLS
EMAIL_TLS_REJECT_UNAUTHORIZED=false

# IMPORTANT: 
# 1. Remplacez 'votre-email@gmail.com' par votre vraie adresse Gmail
# 2. Remplacez 'votre-mot-de-passe-app-gmail' par votre mot de passe d'application Gmail
# 3. Vérifiez que CORS_ORIGIN pointe vers votre domaine Netlify
# 4. Ces variables doivent être configurées sur votre plateforme d'hébergement (Render.com, etc.)
